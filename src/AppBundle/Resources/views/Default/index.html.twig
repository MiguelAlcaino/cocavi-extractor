<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="icon" href="https://afeld.github.io/emoji-css/emoji/space_invader.png" type="image/x-icon" >
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <link href="https://afeld.github.io/emoji-css/emoji.css" rel="stylesheet">
        <style>
            body {
                background-image: url("{{ asset('images/Picnic-Spots-In-Klang-Valley-copy.jpg') }}");
                color: white;
            }
            a:link{
                color: white;
                font-weight: bold;
            }

        </style>

        <title>Cocavi Extractor</title>
    </head>
    <body>
    <div class="container-fluid">
        <h1>Cocaví Extractor!</h1>
        <h6>By <a href="http://miguelalcaino.cl" target="_blank">Miguel Alcaino</a> <i class="em em-rocket"></i></h6>
        <table class="table">
            <tbody>
                {% for podcast in podcasts %}
                    <tr>
                        <td>
                            <a href="{{ podcast.link }}">
                                {{ podcast.date }}
                                <span class="glyphicon glyphicon-download" aria-hidden="true"></span>
                            </a>
                        </td>
                        <td>
                            {% if podcast.megaFileName is defined %}
                                <a href="{{ path('stream_cocavi', {'id': podcast.id}) }}"><span class="glyphicon glyphicon-cloud-download" aria-hidden="true"></span></a>
                            {% endif %}

                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        <nav aria-label="Page navigation">
            <ul class="pagination">
                <li>
                    <a href="{{ path('homepage') }}" aria-label="First">
                        <span aria-hidden="true">&laquo;</span>
                    </a>
                </li>
                {% for page in pages %}
                    <li class="{% if currentPage == page %}active{% endif %}">
                        <a href="{{ path('homepage', { page: page}) }}">{{ page }}</a>
                    </li>
                {% endfor %}
                <li>
                    <a href="{{ path('homepage', {page: lastPage}) }}" aria-label="Last">
                        <span aria-hidden="true">&raquo;</span>
                    </a>
                </li>
            </ul>
        </nav>
    </div>
    <script src="{{ asset('bundles/app/js/zepto.min.js') }}"></script>
    <script src="{{ asset('bundles/app/js/main.js') }}"></script>
    </body>
</html>